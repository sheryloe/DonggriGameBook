export const runEffect = () => {
  throw new Error("Legacy eventRunner is deprecated. Use gameStore runtime actions instead.");
};

export const checkRequirement = () => {
  throw new Error("Legacy eventRunner is deprecated. Use requirements.ts helpers instead.");
};

export const handleChoice = () => {
  throw new Error("Legacy eventRunner is deprecated. Use gameStore.selectChoice instead.");
};
